import { ArgTypes, Description, Meta, Stories } from '@storybook/blocks';

import * as WrapStories from './Wrap.stories.tsx';

<Meta of={WrapStories} />

# Checkbox

<Description of={WrapStories} />
<ArgTypes of={WrapStories} />
<Stories of={WrapStories} />
