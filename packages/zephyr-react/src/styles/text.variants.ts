import clsx from 'clsx';

export const textVariants = {
  fontWeight: {
    body: clsx('font-body'),
    medium: clsx('font-medium'),
    bold: clsx('font-bold'),
    heading: clsx('font-heading'),
  },
  size: {
    xs: clsx('text-xs'),
    sm: clsx('text-sm'),
    md: clsx('text-md'),
    lg: clsx('text-lg'),
  },
};
