import { describe, it } from 'bun:test';

describe('createCrudMethods', () => {
  // TODO: Set up tests for postgres in the future. This has a lot of docker setup required in CI
  // and I can't be bothered to do this right now
  it.todo('should have tests');
});
