import type { GeneratorFunc } from '../src';

const testGenerator: GeneratorFunc = async () => {
  // eslint-disable-next-line no-console
  console.log('Yay, the generator worked!');
};

export default testGenerator;
